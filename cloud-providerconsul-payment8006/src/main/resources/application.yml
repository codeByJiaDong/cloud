server:
  port: 8006
spring:
  application:
    name: cloud-provider-payment
    ### consul 服务注册中心
  cloud:
    consul:
      host: localhost
      port: 8500
      discovery:
        service-name: ${spring.application.name}

